# AGENTS.md

## 사용자 기능·도움말 동기화 규칙

- 신규 사용자 기능은 코드만 구현한 상태로 완료 처리하지 않는다. 기능의 실제 호출어와 사용법이 카카오톡 `헤이봇 도움말`에 반영되고 테스트를 통과해야 구현 완료다.
- 호출어가 있는 기능은 `HeybotSkillCatalog`의 기능 정의와 `userHelpMessages()` 또는 `adminHelpMessages()`에 다음 내용을 함께 기록한다.
  - `헤이봇`을 포함한 정확한 명령 문법
  - 바로 복사해 실행할 수 있는 실제 예시 1개 이상
  - 지원 형식·옵션·기본값
  - 필요한 방 권한과 동작 가능한 방 조건
  - 상태·취소·재시도·재전송·삭제 등 지원되는 작업 제어 명령
- 호출어 없이 자동 동작하는 기능도 자동 실행 조건, 기본 ON/OFF 상태, 관리자 허용·불허용 명령을 도움말에 기록한다.
- 일반 사용법과 관리자 사용법을 분리한다. 관리자 전용 명령은 일반 사용자 도움말에 노출하지 않고 `adminHelpMessages()`에만 기록한다.
- 명령 parser/router와 도움말 문법이 다르면 안 된다. 명령 이름·옵션을 변경할 때는 `BotCommandRouter`, 개별 command parser, `HeybotSkillCatalog`, 운영 문서를 같은 변경에서 함께 갱신한다.
- 도움말 메시지는 카카오톡 전송 한도에 맞춰 메시지당 480자 이하로 나눈다. 페이지 번호와 전체 페이지 수도 함께 갱신한다.
- 도움말 변경 시 최소한 `HeybotSkillCatalogTest`와 `GlmAutoReplyHandlerTest`를 갱신하고 전체 Android 단위 테스트를 실행한다.
- 프록시, 실기기 설정 또는 방 권한이 필요한 기능은 운영 문서에 활성 조건과 실패 시 사용자 안내도 기록한다.

## Android 실기기 정책

- 헤이봇 Android 개발·검증 대상은 PD20 `0123456789ABCDEF`로 고정한다.
- 실기기 조작은 ADB·DB·코드 기반으로 수행하고 Computer Use는 사용하지 않는다.
