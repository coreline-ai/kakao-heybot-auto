# Third-party notices

- yt-dlp: pin the deployed version and SHA-256 in the root-only deployment manifest. The source distribution is documented upstream as Unlicense; review the exact selected artifact and its bundled third-party notices before deployment.
- FFmpeg/ffprobe: supplied externally by the host. Record the exact build and its license configuration before enabling the proxy.

No downloader binary or secret is committed to this repository.
